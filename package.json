{
  "name": "ragt-communication-studio",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "postinstall": "npm install --prefix backend && npm install --prefix frontend",
    "dev": "concurrently \"npm run dev --prefix backend\" \"npm run dev --prefix frontend\"",
    "build": "npm run build --prefix backend && npm run build --prefix frontend",
    "test:e2e": "npm run test:e2e --prefix frontend",
    "test:e2e:ui": "npm run test:e2e:ui --prefix frontend",
    "test:e2e:headed": "npm run test:e2e:headed --prefix frontend"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
